[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# Handle React Router client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200